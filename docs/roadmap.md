# 未来规划

## 当前状态

Core / Runtime / Gateway / Experiment / Config 五层全部完成。扩展轴步骤 1-4 验证通过。Unity 客户端对接中。

---

## 短期（答辩前）

### 1. Unity 客户端联调

- **状态**：进行中（Client 端独立仓库开发）
- **内容**：WS 通信层 + NPC 可视化 + GM 面板 + AutoTestRunner
- **服务端配合**：协议已定义（`docs/protocol.md`），预计无需改动

### 2. 运营平台（Admin）

- **状态**：待规划
- **独立服务**：`NPC-AI-Behavior-System-Admin/`，与游戏服务端通过 MongoDB 解耦
- **技术栈**：Web 前端 + Go/Node 后端 + MongoDB（读写）+ Redis（缓存读取）
- **核心功能**：
  - NPC 类型管理：表单化创建（名称输入、感知范围滑块、状态机模板下拉、行为节点选择）
  - 事件类型管理：威胁等级滑块、传播方式单选、范围滑块
  - FSM 编辑：状态列表管理、转换规则表单（from/to 下拉、优先级、条件组合）
  - BT 编辑：节点类型选择、参数表单、树结构可视化
  - 配置校验：提交前自动检查合法性（状态引用、Key 注册、条件语法）
  - 配置预览：模拟运行验证 NPC 行为
- **用户分类**：
  - 策划/运营：通过 UI 组件（下拉框、滑块、单选）创建配置，不接触技术细节
  - 服务端开发：通过表单化编辑器添加高级配置（有模板、校验、自动补全，不手写裸 JSON）
- **与游戏服务端的关系**：
  - 运营平台随时写 MongoDB，不影响正在运行的游戏
  - 游戏服务端下次启动时自动加载最新配置
  - 更新流程：运营改配置 → 发停服公告 → 到点停服 → 重启服务 → 新配置生效
  - 两者互不调用，通过 MongoDB 数据解耦
- **Redis 使用**：运营平台独立服务，Redis 缓存 MongoDB 查询（多用户频繁读取配置列表），与游戏服务端无关

### 3. 扩展轴步骤 5：NPC 间交互

- **状态**：待规划
- **内容**：警察保护平民、NPC 间信息传递
- **可能的改动**：决策中心扩展（当前只处理事件→NPC，需要支持 NPC→NPC 交互），可能需要新的 BB Key 和 BT 节点
- **风险**：可能需要改 runtime 层代码，需要评估是否违反"加配置不改代码"原则

### 4. 论文数据采集

- **状态**：待执行
- **内容**：运行 experiment 层的定性/定量测试，生成图 1-7 + 表 1 数据
- **命令**：见 `docs/specs/experiment-layer/data-collection-guide.md`
- **前置**：无，可以现在就跑

### 5. 答辩演示准备

- **状态**：待规划
- **内容**：
  - Docker 一键启动演示环境（游戏服务端 + MongoDB + 运营平台）
  - Unity 客户端连接 → spawn NPC → 触发事件 → 观察行为
  - 运营平台演示：策划添加新 NPC 类型 → 停服重启 → 新 NPC 出现
  - 对照实验数据展示

---

## 中期（答辩后优化）

### 6. 更多 NPC 类型和事件

- 补充 NPC 类型：商人（Merchant）、医生（Doctor）、守卫（Guard）
- 补充事件类型：robbery（抢劫）、medical_emergency（医疗紧急）、alarm（警报）
- 通过运营平台添加，验证"策划加配置不改代码"的完整流程

### 7. BT 节点丰富化

- 当前 BT 叶子节点是 stub（占位），返回固定结果
- 实现真实行为节点：move_to（移动到目标）、play_animation（播放动画）、wait（等待）
- 新节点由服务端开发实现，策划通过运营平台使用

### 8. AOI 空间优化

- 当前 world_snapshot 全量广播所有 NPC
- 实现 AOI（Area of Interest）：客户端只收到视野范围内的 NPC
- 实现空间索引：感知过滤从 O(n) 优化为 O(log n)

---

## 长期（如果继续维护）

### 9. 多节点部署

- 当前单节点，所有 NPC 在一个进程中
- 实现分区（Sharding）：按地图区域分配 NPC 到不同节点
- 需要 Redis 做跨节点状态同步（此时游戏服务端 Redis 有真实使用场景）

### 10. NPC 持久化

- 当前 NPC 状态全在内存，服务重启后丢失
- 实现 NPC 状态快照持久化到 MongoDB
- 服务启动时恢复 NPC 状态

### 11. 配置热更新（替代停服更新）

- 当前：改配置需要停服重启
- 实现运营平台一键推送 → 游戏服务端运行时重新加载
- 无需停服即可更新配置（适用于不影响已有 NPC 的小更新）

---

## 系统全景

```
┌──────────────┐     ┌───────────────┐     ┌──────────────┐
│ Unity 客户端  │────→│  游戏服务端    │←────│   MongoDB    │
│  (表现层)     │ WS  │  (AI 引擎)    │启动读│  (配置存储)   │
└──────────────┘     └───────────────┘     └──────┬───────┘
                                                   │ 读写
                     ┌───────────────┐     ┌──────┴───────┐
                     │  运营平台前端   │────→│  运营平台后端  │
                     │  (策划使用)    │HTTP │  + Redis 缓存 │
                     └───────────────┘     └──────────────┘
```

更新流程：运营平台改配置 → 发公告 → 停服 → 重启游戏服务端 → 加载最新配置 → 开服

---

## 不做的事情（明确排除）

| 排除项 | 理由 |
|--------|------|
| 游戏服务端接入 Redis | 单节点内存足够，红线禁止无使用场景提前接入 |
| 游戏服务端认证/鉴权 | 毕设不需要 |
| 消息压缩 | JSON 明文便于调试，毕设规模下性能不是瓶颈 |
| CI/CD | 手动部署足够 |
| 运行时热更新（短期） | 停服更新足够，热更新是长期目标 |
